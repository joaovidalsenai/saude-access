<!DOCTYPE html>
<html lang="pt-br" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styleGenerico.css">
    <link rel="stylesheet" href="/css/perfil.css">
    <title>Meu Perfil - Saúde Access</title>
</head>
<body class="protected">
    <header>
        <h1>Meu Perfil</h1>
    </header>
    <main>
        <div class="perfil-container">
            <div class="perfil-section">
                <div class="perfil-avatar">
                    <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" fill="currentColor" viewBox="0 0 256 256"><path d="M128,24a80,80,0,1,0,80,80A80.09,80.09,0,0,0,128,24Zm0,144a64,64,0,1,1,64-64A64.07,64.07,0,0,1,128,168Z"></path><path d="M231.94,216.78A111.4,111.4,0,0,0,128,144H112a111.4,111.4,0,0,0-103.94,72.78,8,8,0,1,0,15.88,4.44A95.4,95.4,0,0,1,112,160h16a95.4,95.4,0,0,1,88.06,61.22,8,8,0,1,0,15.88-4.44Z"></path></svg>
                </div>
                <h2 class="perfil-nome"><%= user.nome_completo %></h2>
                <p class="perfil-email"><%= user.email %></p>
            </div>

            <div class="perfil-section">
                <h3 class="section-title">Informações Pessoais</h3>
                <div class="perfil-item">
                    <strong>Telefone:</strong>
                    <span id="perfil-telefone"><%= user.telefone %></span>
                </div>
                <div class="perfil-item">
                    <strong>Data de Nascimento:</strong>
                    <span id="perfil-nascimento"><%= user.data_nascimento %></span>
                </div>
                <div class="perfil-item">
                    <strong>CPF:</strong>
                    <span id="perfil-cpf"><%= user.cpf %></span>
                </div>
                <a href="/atualizar-dados" class="btn-editar">Editar Dados</a>
            </div>

            <div class="perfil-section">
                <h3 class="section-title">Endereço</h3>
                 <div class="perfil-item">
                    <strong>CEP:</strong>
                    <span id="perfil-cep"><%= user.endereco.cep %></span>
                </div>
                <div class="perfil-item">
                    <strong>Logradouro:</strong>
                    <span><%= user.endereco.logradouro %>, <%= user.endereco.numero %></span>
                </div>
                <div class="perfil-item">
                    <strong>Bairro:</strong>
                    <span><%= user.endereco.bairro %></span>
                </div>
                <div class="perfil-item">
                    <strong>Cidade/UF:</strong>
                    <span><%= user.endereco.cidade %> - <%= user.endereco.estado %></span>
                </div>
                <a href="/atualizar-endereco" class="btn-editar">Editar Endereço</a>
            </div>
             <div class="perfil-section logout-section">
                <button class="btn-logout">Sair da Conta</button>
            </div>
        </div>
    </main>
    <%- include ('partials/navbar.ejs')%>
    <script src="/js/perfil.js"></script>
    <script src="js/global.js"></script>
</body>
</html>